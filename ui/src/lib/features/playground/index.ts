// Public surface of the playground feature.
// Import these from `$lib/features/playground` rather than reaching into ./components/*.

export { default as PlaygroundChat } from './components/Chat.svelte';
export { default as Completions } from './components/Completions.svelte';
export { default as Notes } from './components/Notes.svelte';
